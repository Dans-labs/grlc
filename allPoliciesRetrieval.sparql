#+ endpoint: http://portal.odissei.nl:7200/repositories/margheritatest

PREFIX odrl: <http://www.w3.org/ns/odrl/2/>

select ?doi ?policy 
where { 
    ?doi odrl:hasPolicy ?policy .
    FILTER (?doi = ?_doi_iri)
} 
